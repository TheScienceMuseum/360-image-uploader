[
  {{#each Items}}
    {
      "title": "{{title.S}}",
      "id": "{{objectId.S}}",
      "path": "{{../bucket}}/{{id.S}}",
      "active": {{active.BOOL}},
      "modification_date": "{{modificationDate.S}}"
    }{{#if @last}}{{else}},{{/if}}
  {{/each}}
]
